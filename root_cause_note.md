Root cause found in `/opt/data/git_projects/Artist-Synch/program_files/settings_manager.py`:
In the `save_band` function, `save_settings(settings)` was commented out on line 200 (leaving `#save_settings(settings)`), and instead manually performed file writing starting at line 201. However, if a new band was created (`is_new=True`), `settings["bands"].append(band)` updates the in-memory `settings` object, but if `main_location` was entered manually via the entry box (instead of using the "Browse..." button), `band["main_location"]` remains a string rather than a dictionary. This causes issues because `band["main_location"]["path"] = main_loc_path_var.get()` on line 187 throws a `TypeError: string indices must be integers, str` when `band["main_location"]` is a plain string.
